<div id="<%= dom_id item %>">
  <h2><%= item.title %></h2>
  <p><%= item.content %></p>
  <% if item.tags.any? %>
    <p>
      <% item.tags.each do |tag| %>
        <span style="display: inline-block; padding: 2px 8px; margin: 2px; border: 1px solid #999; border-radius: 4px; font-size: 0.85em;"><%= tag.name %></span>
      <% end %>
    </p>
  <% end %>
</div>
